services:
  db:
    build: ./data
     
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    depends_on:
      - db
  
  frontend:
    build: ./frontend
    ports:
      - "8094:80"
    depends_on:
      - backend